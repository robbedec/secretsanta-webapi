import { PresentFilterPipe } from './present-filter.pipe';

describe('PresentFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PresentFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
